**<2026 8/3>**

**문제 1**

***조합회로(combinational circuit)와 순차회로(sequential circuit)의 가장 근본적인 차이는 무엇일까요? "회로 안에 저장 요소가 있다/없다" 같은 표면적인 답 말고, 왜 그 차이가 회로의 "출력이 결정되는 방식"에 영향을 주는지까지 설명해주실 수 있나요?***

조합회로 - 현재 입력값들에 의해서만 출력값 정해짐. 기억소자나 피드백 루트 없고 클록 신호가 불필요하다.
순차회로 - 현재 입력값들 + 과거 입력값으로 출력값 정해짐. 기억소자가 존재하고 클록 신호가 필요하다.

조합회로는 현재 입력값만 필요한데 순차회로는 과거의 입력값도 필요하다. 그래서 순차회로는 시간에 걸친 동작을 표현할 수 있다. 그리고 이게 나중에 왜 순차회로에 단계마다 레지스터를 추가해야하는가로 이어진다.

**문제 2 (직관 문제)**

***아래와 같은 상황을 생각해봅시다: 어떤 회로에 클럭 신호가 들어가는 D 플립플롭이 있습니다. 이 플립플롭의 입력(D)에 연결된 조합회로의 지연시간(propagation delay)이 너무 길어지면, 회로에 어떤 문제가 생길까요?***

플립프롭은 클럭 엣지가 오기전에 일정한 시간동안 값이 안정적으로 유지 되어 있어야하는데(setup time), 
만약 조합회로의 지연시간이 너무 길면 클록 엣지일때 입력값이 확정되지 않고 흔들려 값이 부정확하게 들어가는 setup time violation 상태에 빠질 수 있다.
-> 파이프라인의 클럭 주기 결국 가장 긴 조합회로 경로의 지연시간에 의해서 결정된다(다른 요소들도 있지만 우선 이거).

클록 주기 >= 플립플롭의 clock-to-Q 지연 + 조합회로 지연 + 다음 플립플롭의 setup time

이 공식을 추가 설명하자면 예를들어 플리플롭A -> 조합 회로 -> 플립플롭B 이런구조라고할때, t=0 클럭 엣지 발생시 FF A가 자기 내부에 저장된 값을 출력 Q로 내보내기 시작함. t = clock-to-Q 드디어 FF A의 출력 Q에 새값이 안정적으로 나타남. 이 Q값이 조합 회로에 입력으로 들어가 조합 회로를 통과(이때도 시간걸림). FF B 입장에서 자기의 다음 클럭 엣지가 오기전 setup time 필요.

---

**<2026 8/4>**

**문제1**

***Setup time이 "클럭 엣지가 오기 직전에 D 입력이 안정적이어야 하는 최소 시간"이라면, hold time은 무엇일까요? 그리고 왜 이게 setup time과 별개로 필요할까요?***

setup time: 클럭 엣지 이전에 D가 안정적이어야 하는 시간
Hold time: 클럭 엣지 이후에도 D가 안정적이어야 하는 시간 -> 클럭 엣지 직후에 D에 값이 너무 빨리 바뀌면 플립플롭이 어떤 값을 붙잡았는지 애매해지는 hold time violation 발생.

**문제 2**

***아래 상황을 생각해봅시다:
어떤 회로 설계자가 "우리 회로의 클럭 속도를 2배로 올리고 싶다"고 합니다. 배운 내용을 바탕으로, 이 목표를 달성하기 위해 회로 관점에서 무엇을 줄여야 하는지, 그리고 실제로 그걸 줄이는 설계 기법이 뭐가 있을지 짐작 가는 대로 자유롭게 말씀해주세요.***

클록 주기 공식에서 setup time, hold time 회로의 자체 특성이라 줄일수 없기때문에 조합 회로에 변화를 주어야한다. 조합 회로의 지연을 줄이기 위해선 경로 중간에 레지스터를 끼워넣어 한 사이클에 처리하는 조합 회로의 양을 줄이는 파이프라이닝 기법을 사용한다.

A → B → C 를 한 클럭 사이클에 다 처리 => 클럭 주기 ≥ clk-to-Q + (A+B+C 지연) + setup

A → 레지스터 → B → C 로 두 단계로 나눔 => 클럭 주기 ≥ clk-to-Q + (A 지연만) + setup   ← 훨씬 짧아짐!

1클럭: A 처리 후 레지스터에 저장
2클럭: B→C 처리

=> 클럭 주기를 짧게 만드는 이유?

클럭 주기와 클럭 주파수는 역수 관계로 주기가 짧아지면 주파수가 높아지고 1초에 더 많은 클럭 사이클이 지나가 1초에 더 많은 연산을 처리할 수 있다.

---

**<2026 8/5>**

**개념 - MIPS 5단계 파이프라인**

RISC 계열 명령어셋    
IF (Instruction Fetch - 메모리에서 실행할 명령어 가져오기) → ID (Instruction Decode - 가져온 명령어 해석) → EX (Execute - 실제 계산 실행) → MEM (Memory Access - 필요시 메모리에 값을 읽거나 쓴다) → WB (Write Back - 계산 결과를 레지스터에 다시 저장)   

**문제 1**

***이 5단계로 나누는 이유는?***

이 5단계로 나누는 것이 항상 정답은 아니지만, 5단계가 균형점인 이유는 각 단계(IF/ID/EX/MEM/WB)가 하는 일의 성격이 다르고, 각각 필요한 조합회로 지연시간이 얼추 비슷하게 맞춰지도록 설계 되었기 때문이다. 여기서 임의로 2단계를 하나의 파이프라인으로 합치려고하면 조합회로 지연시간이 길어지고 클럭 주기가 그 느린 단계로 맞춰지기 때문에 전체적으로 느려진다. 

실제 상용 CPU들은 상황과 목적에 따라 다른 단계에 파이프라인을 사용하지만 여기서 주목할점은 가장 느린 단계가 클럭 주기를 결저하고 레지스터를 많이 설치하면 할 수록 트레이드 오프가 생긴다는 점이다.

---

**<2026 8/6>**

**문제 1**

아래 두 명령어가 연속으로 실행된다고 해봅시다:

```
명령어 1: add $1, $2, $3     # $2 + $3 결과를 $1에 저장       
명령어 2: sub $4, $1, $5     # $1 - $5 결과를 $4에 저장
```

명령어 2는 명령어 1의 결과값($1)을 바로 사용합니다. 그런데 5단계 파이프라인에서는 명령어 1과 명령어 2가 겹쳐서(overlap) 실행됩니다 — 명령어 1이 아직 EX나 MEM 단계에 있을 때, 명령어 2는 이미 ID 단계에서 $1 값을 읽으려고 시도할 수 있어요.

**질문 1**

***이 상황에서 왜 문제가 생길 수 있을까요? 그리고 이런 종류의 문제를 뭐라고 부를 것 같나요?***

데이터 해저드  
RAW(Read After Write) - 쓰기가 일어나기 전에 읽기를 시도해 생긴 문제

<img width="338" height="84" alt="스크린샷 2026-08-06 오후 5 04 15" src="https://github.com/user-attachments/assets/c5d85cee-763c-4e18-aa89-1a05da5d3159" />    

위 그림처럼 명령어 2가 사이클 3단계에 $1의 값을 읽으려고 하는데 아직 값이 정해지지 않았기 때문에 새 계산 기록이 반영되지 않은 오래된 값이 들어있다. 그 결과 명령어 2는 잘못된 값을 가지고 계산을 시작하기 때문에 최종결과가 틀리게 된다.

이러한 문제를 해결하기 위해 대표적으로 forwarding/bypassing 등의 기법을 사용한다.(EX 단계에서 계산된 결과를 굳이 레지스터에 쓰지 않고 다음 명령어의 EX 단계로 바로 넘겨주는 방식)

**문제 2**    

<img width="121" height="125" alt="스크린샷 2026-08-06 오후 5 13 32" src="https://github.com/user-attachments/assets/6a952133-e342-4b99-aafd-cf7d834d6bf7" />    

이게 어셈블리로 번역되면 분기(branch) 명령어가 생깁니다. 파이프라인은 IF 단계에서 "다음에 실행할 명령어"를 미리 가져와야 하는데, if (x > 0)을 계산하는 결과(참/거짓)는 그보다 나중 단계(EX 정도)에서야 확정됩니다.

**질문 1**

***그럼 IF 단계는 분기의 결과를 아직 모르는 상태에서 "다음 명령어"를 어떻게 미리 가져올 수 있을까요? 이 상황에서 어떤 문제가 생길 수 있을지 짐작해서 말씀해주세요.***

<img width="599" height="139" alt="스크린샷 2026-08-06 오후 5 47 22" src="https://github.com/user-attachments/assets/1a8582c1-7c86-4f2a-9a52-6eb5527b6843" />

분기의 결과를 아직 모르는 상태에서 다음 명령어를 예측한다. 실제 프로그램에서 분기는 어느 정도 예측 가능한 패턴을 보이기때문에 "분기 예측(branch prediction)" 기법을 사용해서 추측이 맞았을때 이득을 볼 수 있고, 설명 추측이 틀렸다고 하더라도 사이클 4에 진짜 명령어 IF 가 시작하기 때문에(어짜피 예측 안하면 기다려야하니까) 타이밍적으로 손해를 보지 않을수있는 구조이다. 

문제 1번과 다른 점은, 문제 1번은 다음에 실행할 명령어가 뭔지는 확실히 알지만 아직 그 명령어가 필요로하는 값이 준비가 안된 상황이고, 문제 2번 상황은 다음에 실행할 명령어가 애초에 뭔지조차 모르는 상황이다.

---

**<2026 8/10>**

**문제 1**

지난 예시를 다시 가져와볼게요:

```
명령어 1: add $1, $2, $3  
명령어 2: sub $4, $1, $5
```

포워딩은 "EX 단계에서 계산된 결과를, 레지스터에 쓸 때까지 기다리지 않고 다음 명령어의 EX 단계로 지름길을 뚫어서 바로 넘겨준다"고 배웠습니다.

**질문** 

그런데 만약 이렇게 바로 다음 명령어가 아니라, 명령어가 하나 더 사이에 껴서 아래처럼 된다면 어떨까요?

```
명령어 1: add $1, $2, $3  
명령어 2: or  $6, $7, $8      (이건 $1과 무관)  
명령어 3: sub $4, $1, $5      (이제 $1을 씀)
```

***이 경우, 명령어 3이 $1을 쓰는 시점(ID)과 명령어 1이 $1을 계산하는 시점(EX) 사이의 "간격"이 아까보다 넓어졌습니다. 이 경우에도 포워딩이 여전히 필요할까요? 아니면 이 정도 간격이면 자연스럽게 문제가 해결될까요?***

```
사이클:        1    2    3    4    5    6    7     
명령어1(add):  IF   ID   EX   MEM  WB     
명령어2(or):        IF   ID   EX   MEM  WB   
명령어3(and):            IF   ID   EX   MEM  WB   
명령어4(sub):                 IF   ID   EX   MEM  WB   
```

표를 직접 살펴봤을 때, 명령어 3이 사이클 4에 $1을 읽어오는데 명령어 1은 사이클 5가되어야 $1 값을 쓴다. 즉, 읽는 시점이 쓰는 시점보다 여전히 빨라서 포워딩이 필요하다. 여기서 얻어가야할 부분은 명령어 사이의 간격이 벌어질수록 해저드가 자연스럽게 완화되긴 하지만, 몇 칸 이내에서는 여전히 포워딩 같은 명시적 대응이 필요하다.

**문제 2**

분기 예측이 "추측"이라고 했는데, 이 추측을 어떻게 할지는 CPU 설계자가 정해야 합니다. 가장 단순한 전략 중 하나가 **"분기는 항상 예측한 대로 간다고 가정하되, 아주 단순한 규칙 하나로 예측한다"**는 겁니다.

예를 들어 이런 규칙을 생각해볼 수 있어요: "분기는 항상 실행 안 된다(not-taken)고 예측한다" (즉 항상 else 쪽, 혹은 분기 안 하고 다음 줄로 그냥 진행한다고 찍는 전략).

**질문** 

이 단순한 전략이 실제 프로그램에서 꽤 잘 맞는 경우가 있는데, 어떤 상황(어떤 종류의 코드 패턴)에서 이 전략이 잘 맞을 것 같나요? 반대로 이 전략이 자주 틀릴 것 같은 코드 패턴은 뭐가 있을까요? (프로그래밍하면서 봤던 for/while 반복문, if문 등을 떠올려보세요)

예측은 정말 상황에 따라 정적 예측(static prediction), 동적 예측(dynamic prediction) 등으로 다양하게 할 수 있는데 BTFNT는 정적 예측 중 하나이다.

BTFNT = backward taken, forward not-taken

```
loop_start:
    print(i);
    i++;
    if (i < 3) goto loop_start;
```

for/while 반복문을 어셈블리로 풀어보면 "루프 조건이 맞으면 다시 코드위로 가서 명령어를 실행" 하는 구조가 된다. 이러한 분기를 backward로 간다고 부르고 for/while 루프는 대부분 참이 되는 조건이 많고 마지막에만 거짓이 되기때문에 무조건 실행될 거다 라고 예측한다.

```
if (error_occurred) {  
    handle_error();         
}
normal_code();
```

반대로 if 구문에서는 보통 코드가 위에서 아래로 실행되는, 아래쪽으로 건너뛰는 구조이고 이를 forward 라고한다. 예시처럼 에러체크의 if 구문은 에러가 안날거라고 미리 예측하고 가기때문에 실행이 안될거라고 예측한다.

--- 

**<2026 8/13>**

**문제 1**

가장 단순한 동적 예측 방식은 "1-bit predictor"**입니다. 아이디어는 이래요:

분기 하나마다 비트 하나를 기억해둔다 (0 = "지난번엔 not-taken이었다", 1 = "지난번엔 taken이었다"). 그리고 다음번엔 **"저번이랑 똑같을 것"**이라고 예측한다.

**질문**

***이 방식은 "저번 결과를 그대로 다음 예측으로 쓴다"는 건데, 왜 이게 코드 구조만 보고 찍는 정적 예측(BTFNT 등)보다 더 정확할 수 있을까요? (힌트: 같은 분기 명령어라도 실행될 때마다 항상 같은 결과가 나올까요, 아니면 상황에 따라 다를 수 있을까요? for문을 예로 들어 생각해보세요.)***

1-bit predictor -> 분기가 실제로 과거에 어떻게 됐었는지 직접 관찰해서 찍어서 정적 예측보다 더 정확도가 높다고 하는데 사실 여기에 맞는 예시를 못찾겠어서 아직 잘 모르겠지만 일단 그렇다고는 함.

**문제 2**

아래 코드를 봅시다:

```
for (int j = 0; j < 5; j++) {
    for (int i = 0; i < 3; i++) {
        print(i);
    }
}
```

안쪽 for문(i < 3)은 바깥쪽 루프가 한 번 돌 때마다 "i=0,1,2로 3번 참(taken) → i=3에서 1번 거짓(not-taken)"을 반복합니다. 이게 바깥쪽 루프(j) 때문에 총 5번 반복됩니다.

**질문**

***1-bit predictor를 안쪽 for문의 분기에 적용한다고 할 때, 각 바깥쪽 루프가 새로 시작할 때마다(즉 안쪽 루프가 다시 0부터 시작할 때마다) 예측이 맞을지 틀릴지 짐작해보세요. 특히 "안쪽 루프가 끝나고(i=3, not-taken) 다시 새 바깥 루프가 시작해서 i=0이 될 때(taken)" 이 경계 지점에서 무슨 일이 생길지 생각해보세요.***

처음을 T로 시작했다고 가졍했을때 아래와 같은 결과가 나온다.       
<img width="650" height="764" alt="스크린샷 2026-08-18 오후 5 08 43" src="https://github.com/user-attachments/assets/d557d699-6e21-4fc7-9fb9-e23e4535df47" />

결과값을 살펴보면 1-bit predictor에 정확성은 특히 안쪽 루프가 끝나고 다시 새 바깥 루프가 시작될때 무조건 틀리기때문에 정확도가 많이 떨어진다.

이러한 문제를 해결하기위해 2-bit predictor를 사용하기도한다. 2-bit predictor는 이름과 같이 이전 2단계를 기억하는것으로: 11(strong taken), 10(weakly taken), 01(weakly not taken), 00(strongly not taken) 4개의 단계로 좀 더 세분화 시켜서 저장한다.          
<img width="642" height="763" alt="스크린샷 2026-08-18 오후 5 10 28" src="https://github.com/user-attachments/assets/33f97ae1-4b5c-4947-87d6-1ea88c81a626" />

두개의 결과값을 비교하면 2-bit predictor의 정확도가 확연히 높은 것을 확인할 수 있다. 하지만 위 예시들은 동적 예측이라는 큰 카테고리 안에 어떤 구체적인 기법들이 존재하나 소개하는 것에 가깝고 상황에 따라 맞는 동적 예측을 하는 것이 중요하다.

---

**<2026 8/18>**

**문제 1** 

CPU가 메모리(RAM)에서 데이터를 가져오는 속도는, CPU가 연산하는 속도보다 훨씬 느립니다 (수십~수백 배 차이가 나기도 해요). 그래서 CPU와 메모리 사이에 캐시(cache)라는 작고 빠른 저장 공간을 둡니다.

**질문** 

***캐시는 메모리 전체 내용을 다 담을 수 없을 만큼 작습니다 (메모리가 수십 GB라면 캐시는 수십 MB 정도예요). 그런데도 캐시를 쓰면 실제로 프로그램이 꽤 빨라집니다. 왜 이렇게 작은 저장 공간만으로도 효과가 있는 걸까요?***

시간적 지역성 -> 방금 쓴 데이터를 곧 또 쓴다(ex: 반복문의 카운터 변수)      
공간적 지역성 -> 방금 쓴 주소 근처를 곧 또 쓴다(ex: 배열 순회)     

캐시가 메모리 전체를 담을 만큼 클 필요가 없는 이유는, 실제 프로그램들이 두 가지 지역승을 강하게 보이기 때문이다. 프로그램이 메모리 전체를 무작위로 여기저기 접근하는게 아니라, 대부분 "최근에 쓴 것" 혹은 "그 근처" 를 계속 다시쓰기 때문이다. 이걸 캐시 히트(cache hit)라고 하고 반대는 캐시 미스(cache miss)라고한다.

**문제 2** 

```
// 코드 A
for (int i = 0; i < 1000; i++)
    for (int j = 0; j < 1000; j++)
        sum += arr[i][j];

// 코드 B
for (int j = 0; j < 1000; j++)
    for (int i = 0; i < 1000; i++)
        sum += arr[i][j];
```

배열은 메모리에 행(row) 단위로 연속하게 저장됩니다 (즉 arr[0][0], arr[0][1], arr[0][2], ..., arr[0][999], arr[1][0], ... 이런 순서로 메모리 주소가 이어집니다).























